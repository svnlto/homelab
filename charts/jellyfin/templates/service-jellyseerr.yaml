---
apiVersion: v1
kind: Service
metadata:
  name: jellyseerr
  annotations:
    {{- range $key, $val := .Values.global.annotations }}
    {{ $key }}: {{ $val | quote }}
    {{- end }}
spec:
  ports:
    - port: {{ .Values.jellyseerr.port }}
      targetPort: {{ .Values.jellyseerr.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "jellyfin.selectorLabels" (dict "name" "jellyseerr") | nindent 4 }}
