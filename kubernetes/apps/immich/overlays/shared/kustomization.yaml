---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich
resources:
  - namespace.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: immich-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
