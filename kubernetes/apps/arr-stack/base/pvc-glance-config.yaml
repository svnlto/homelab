---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glance-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: arr-glance-config
  resources:
    requests:
      storage: 1Gi
