---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glance-config
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: arr-glance-config
  resources:
    requests:
      storage: 1Gi
