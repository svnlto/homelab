---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-nfs-fast
  volumeName: arr-prowlarr-config
  resources:
    requests:
      storage: 2Gi
