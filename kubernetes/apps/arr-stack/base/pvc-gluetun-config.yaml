---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gluetun-config
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-nfs-fast
  volumeName: gluetun-config
  resources:
    requests:
      storage: 1Gi
