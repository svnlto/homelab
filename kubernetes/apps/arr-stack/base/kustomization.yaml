---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Namespace
  - namespace.yaml

  # Static NFS PVs (media + scratch)
  - pv-media-movies.yaml
  - pv-media-tv.yaml
  - pv-media-music.yaml
  - pv-media-books.yaml
  - pv-scratch.yaml

  # Static CSI PVs (app config)
  - pv-gluetun-config.yaml
  - pv-sonarr-config.yaml
  - pv-radarr-config.yaml
  - pv-lidarr-config.yaml
  - pv-bazarr-config.yaml
  - pv-prowlarr-config.yaml
  - pv-sabnzbd-config.yaml
  - pv-qbittorrent-config.yaml
  - pv-slskd-config.yaml
  - pv-recyclarr-config.yaml
  - pv-buildarr-config.yaml
  - pv-glance-config.yaml

  # Static NFS PVCs (media + scratch)
  - pvc-media-movies.yaml
  - pvc-media-tv.yaml
  - pvc-media-music.yaml
  - pvc-media-books.yaml
  - pvc-scratch.yaml

  # Static config PVCs
  - pvc-gluetun-config.yaml
  - pvc-qbittorrent-config.yaml
  - pvc-sabnzbd-config.yaml
  - pvc-slskd-config.yaml
  - pvc-prowlarr-config.yaml
  - pvc-radarr-config.yaml
  - pvc-sonarr-config.yaml
  - pvc-lidarr-config.yaml
  - pvc-bazarr-config.yaml
  - pvc-recyclarr-config.yaml
  - pvc-buildarr-config.yaml
  - pvc-glance-config.yaml

  # ConfigMaps
  - configmap-recyclarr.yaml
  - configmap-buildarr.yaml
  - configmap-glance.yaml

  # External Secrets
  - external-secret.yaml

  # Downloaders (Gluetun + qBit + SABnzbd + Slskd + Prowlarr + FlareSolverr)
  - downloaders-deployment.yaml
  - downloaders-service.yaml

  # Arr apps
  - radarr-deployment.yaml
  - radarr-service.yaml
  - sonarr-deployment.yaml
  - sonarr-service.yaml
  - lidarr-deployment.yaml
  - lidarr-service.yaml
  - bazarr-deployment.yaml
  - bazarr-service.yaml

  # Config tools
  - recyclarr-cronjob.yaml
  - buildarr-deployment.yaml

  # Dashboard
  - glance-deployment.yaml
  - glance-service.yaml
