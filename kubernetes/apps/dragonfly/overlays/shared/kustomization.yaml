---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cache
resources:
  - namespace.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: dragonfly-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
