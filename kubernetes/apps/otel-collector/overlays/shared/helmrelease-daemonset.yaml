---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: otel-agent
  namespace: otel-collector
spec:
  interval: 30m
  releaseName: otel-agent
  chart:
    spec:
      chart: opentelemetry-collector
      sourceRef:
        kind: HelmRepository
        name: opentelemetry
      interval: 1h
  valuesFrom:
    - kind: ConfigMap
      name: otel-agent-values
