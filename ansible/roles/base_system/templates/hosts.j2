# /etc/hosts - Managed by Ansible
# {{ ansible_managed }}

127.0.0.1       localhost

# Proxmox VE requires the hostname to resolve to a non-loopback IP
# During image build, we use the current IP; this should be updated post-deploy
{{ ansible_default_ipv4.address | default('127.0.1.1') }}    {{ node_hostname }}.{{ node_domain }} {{ node_hostname }}

# IPv6
::1             localhost ip6-localhost ip6-loopback
ff02::1         ip6-allnodes
ff02::2         ip6-allrouters
